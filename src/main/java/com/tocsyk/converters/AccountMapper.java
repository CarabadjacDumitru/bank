package com.tocsyk.converters;

public class AccountMapper /*  implements RowMapper<Account>*/ {
/*
    @Override
    public Account mapRow(ResultSet rs, int rowNum) throws SQLException {


        float balance = rs.getFloat("balance");
        float creditscore = rs.getFloat("creditscore");
        int account_status = rs.getString("account_status");
        int addressID = rs.getInt("account_contract");

        return new Account(mobile, telephone, email, addressID);
    }
*/
}
